plugins {
    id 'java'
}
version '1.0-SNAPSHOT'

dependencies {

    implementation(
            "jakarta.inject:jakarta.inject-api:${jakartaInjectVersion}"
    )

    testImplementation(
            "jakarta.inject:jakarta.inject-tck:${jakartaInjectVersion}"
    )
}
